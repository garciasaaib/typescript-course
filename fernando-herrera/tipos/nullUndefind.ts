(() => {

  // this value only can be undefinded
  let nada: undefined = undefined

  // other types cannot be undefinded
  let nadaBad: number = undefined

// null !== undefined

  console.log(nada);
})()